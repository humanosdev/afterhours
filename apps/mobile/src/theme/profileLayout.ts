/** Profile route spacing — mapped from `apps/web` profile page Tailwind. */
export const profileLayout = {
  avatarColWidth: 84,
  gridGapX: 16,
  identityTop: 20,
  nameUnderAvatarTop: 10,
  bioTop: 16,
  actionsTop: 16,
  actionsGap: 8,
  actionHeight: 44,
  actionRadius: 10,
  tabsTop: 20,
  tabGap: 20,
  tabPadBottom: 10,
  tabContentTop: 12,
  /** Four list rows — profile Venues tab skeleton reserve. */
  venuesListMinHeight: 224,
  menuWidth: 208,
  menuRadius: 16,
  statValueSize: 18,
  statLabelSize: 11,
  venuePillText: 11,
} as const;
