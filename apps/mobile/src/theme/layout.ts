/** Shared layout rhythm — aligned with web/PWA density. */
export const layout = {
  screenPaddingX: 16,
  screenPaddingTop: 8,
  screenPaddingBottom: 12,
  tabBarClearance: 80,
  sectionGap: 14,
  cardGap: 10,
  cardRadius: 14,
  glassRadius: 16,
  rowPaddingY: 10,
} as const;
