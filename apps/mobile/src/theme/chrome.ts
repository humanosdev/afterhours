/** Shared chrome rhythm — one product surface language across routes. */
export const chrome = {
  pageHeaderBorder: "rgba(255, 255, 255, 0.08)",
  listDivider: "rgba(255, 255, 255, 0.08)",
  /** Hairline only — avoid heavy grey boxes on atmospheric pages. */
  hairlineWidth: 1,
} as const;
