/** PWA map auto-tour timing — `apps/web/src/app/map/page.tsx` (native overrides dwell/idle). */
export const AUTO_TOUR_IDLE_GRACE_MS = 16_000;
/** Dwell on each venue before the next auto hop. */
export const AUTO_TOUR_REPEAT_MS = 6_000;
/** Manual checkpoint arrows — ignore auto-advance briefly after arrow tap. */
export const AUTO_TOUR_ARROW_GRACE_MS = 2200;
export const CHECKPOINT_ARRIVAL_PULSE_MS = 1600;
