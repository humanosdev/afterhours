import { DiscoverySearchScreen } from "../../src/components/discovery/DiscoverySearchScreen";

/** PWA `/search` — global discovery overlay. */
export default function SearchDiscoveryScreen() {
  return <DiscoverySearchScreen />;
}
