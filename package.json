{
  "name": "intencity",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "strip-pwa-dev": "npm run strip-pwa-dev -w web",
    "dev": "npm run dev -w web",
    "dev:turbo": "npm run dev:turbo -w web",
    "dev:clean": "npm run dev:clean -w web",
    "dev:reset": "npm run dev:reset -w web",
    "icons:generate": "npm run icons:generate -w web",
    "splash:process": "npm run splash:process -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "purge-scheduled-accounts": "npm run purge-scheduled-accounts -w web"
  }
}
